// You are trying to put a hash in ruby or an object in javascript into an 
// array, but it always returns error, solve it and keep it as simple 
// as possible!

var items, object;

items = [];
object = {a: 'b', c: 'd'};

items.push(object);
